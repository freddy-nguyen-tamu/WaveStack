# Coverage Dashboard

This folder is a starter dashboard for combined test coverage across WaveStack:

- Vitest coverage from the React frontend
- Jest coverage from the NestJS GraphQL API
- Pytest coverage from the Python audio and AI service
- xUnit coverage from the C#/.NET analytics service

Run the service coverage commands first, then render:

```txt
npm --prefix coverage-dashboard run render
```
